apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp-final1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: myapp-final1
  template:
    metadata:
      labels:
        app: myapp-final1
    spec:
      containers:
        - name: myapp-final1
          image: ghcr.io/deepak1067/myapp/myimage:latest
          ports:
            - containerPort: 8080
          env:
          # Add environment variables here, if needed
          resources:
          # Set resource limits and requests, if needed
      imagePullSecrets:
        - name: dockerconfigjson-github-com
---
apiVersion: v1
kind: Service
metadata:
  name: myapp-service-final1
spec:
  selector:
    app: myapp-final1
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
